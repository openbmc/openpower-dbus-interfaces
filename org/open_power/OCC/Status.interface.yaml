description: >
    Definition of OCC status sensor, Implement to indicate whether OCC is
    active on a processor on not.
properties:
    - name: OccActive
      type: boolean
      description: >
        Indicates whether OCC is active or not
